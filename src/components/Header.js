import PropTypes from 'prop-types'
// Allows you to look at the route you're currently on

// const Header = ({onAdd, showAdd, setStatus}) => {

    
//     return (
//     )
// }


// == Default Prop, but will be overridden if you pass in the props directly ==
// Header.defaultProps = {
//     title: 'Task Tracker'
// }

// == PropTypes, allows for more robust code and catch errors
// - Need to import PropTypes (impt)
// Header.propTypes = {
//     title: PropTypes.string.isRequired
// }

// == CSS, inline CSS or create a CSS Object and pass it in
// - Pass it in as style={headingStyle}
// const headingStyle = {
//     color: 'red'
// }

// export default Header
